# npm run dev — mặc định gọi BE trên máy (Spring local :8080)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Muốn dev mà gọi server xa: tạo .env.local và ghi NEXT_PUBLIC_API_URL=... (ghi đè file này)
